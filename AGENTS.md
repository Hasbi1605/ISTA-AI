# AGENTS.md — ISTA AI (Magang-Istana)

## Tentang repo
Monorepo hybrid **Laravel (Livewire/Volt + Blade + Alpine.js) + python-ai (FastAPI)** untuk
asisten AI atas dokumen privat pegawai Istana Kepresidenan Yogyakarta. `laravel/` melayani web
UI, auth/otorisasi, upload, metadata, admin UI, queue, dan callback OnlyOffice; `python-ai/`
melayani chat streaming (SSE), ingest dokumen, RAG, model routing, embeddings, summarization,
export, dan memo. Data di MySQL + Redis; vektor di ChromaDB. Konfigurasi AI tunggal di
`python-ai/config/ai_config.yaml`.

## WAJIB dibaca dulu (context)
Sebelum mengubah kode, baca dokumen konteks ini agar tidak eksplorasi ulang:
1. `docs/CODEBASE-CONTEXT.md` — peta backend (Laravel + Livewire) & AI service (python-ai), fitur, dan flow user/admin. **Acuan utama.**
2. `PRD-ISTA-AI.md` — produk, requirements, core features, user/admin flow, arsitektur, skema DB, constraints.
3. `README.md` — gambaran produk, arsitektur hybrid, dan setup. `docs/` lain bila relevan (`data-flow-privacy.md`, `production-*`, `deploy-*`).

Jika perubahanmu mengubah struktur folder, service, route, Livewire/Blade, router/endpoint
python-ai, atau flow, **perbarui `docs/CODEBASE-CONTEXT.md`** pada bagian yang relevan di perubahan yang sama.

## Cara bekerja
- Untuk tugas kompleks, mulai dengan plan singkat di percakapan sebelum menulis kode. Repo ini tidak memakai folder `issue/` atau PR flow lagi.
- Setelah verifikasi memadai, perubahan boleh langsung di-commit ke `main` dan push ke remote. Setelah push berhasil, laporkan hasilnya lalu stop; jangan menunggu workflow CI/CD atau deploy production selesai kecuali user meminta eksplisit.
- Gunakan perubahan sekecil mungkin yang menyelesaikan masalah. Jangan refactor besar
  kecuali diminta atau benar-benar diperlukan.
- Ikuti pola yang sudah ada: thin controller, **UI interaktif di Livewire**, logika domain di
  Service, `AIService` sebagai jembatan HTTP ke python-ai, prompt/model diatur lewat
  `ai_config.yaml` (bukan hardcode). Chat memakai **SSE** (`EventSource`), bukan Echo/WebSocket.
- python-ai: prompt sebagai single source of truth di `config/ai_config.yaml`; jaga pipeline
  RAG (chunk/embed/PDR/retrieval) dan cascade model tetap konsisten.
- Jika ada ketidakpastian, nyatakan asumsi secara eksplisit.

## Verifikasi wajib
Setiap perubahan kode wajib diverifikasi pada area terdampak.
- **Laravel:** jika menyentuh `laravel/`, jalankan `cd laravel && php artisan test` (atau filter test relevan).
- **Python:** jika menyentuh `python-ai/`, aktifkan virtualenv lalu jalankan
  `cd python-ai && source venv/bin/activate && pytest` (atau test relevan).
- Jika perilaku penting berubah tetapi test belum ada, tambahkan test dulu.
- Jangan anggap tugas selesai bila test relevan belum dijalankan. Untuk perubahan lintas-layanan,
  jalankan verifikasi penuh kedua sisi (Laravel + Python) sebelum dianggap selesai.

### Perintah cepat
```bash
cd laravel && php artisan test                       # test Laravel
cd python-ai && source venv/bin/activate && pytest   # test Python (aktifkan venv dulu)
cd laravel && php artisan serve                      # web app
cd laravel && npm run dev                            # Vite (Livewire/Alpine assets)
cd python-ai && source venv/bin/activate && uvicorn app.main:app --host 127.0.0.1 --port 8001  # AI service
```

## Keamanan (jaga selalu)
- python-ai dilindungi bearer token (`verify_token`); jangan log/echo isi prompt, dokumen, atau
  rahasia. `AIService` sudah meredaksi rahasia di log — pertahankan.
- Token AI, secret OnlyOffice/JWT, kredensial DB, OAuth, dan API key provider hanya di `.env`
  lokal/deploy. Jangan commit `.env` nyata, dokumen produksi, dump DB, service-account JSON, atau data Chroma.
- Akses dokumen via signed URL, private disk, dan otorisasi server-side. Baca `docs/data-flow-privacy.md` sebelum memakai data nyata.
- Endpoint admin harus tetap di belakang `admin` / `super_admin` / `admin.password_changed` sesuai kebutuhan. Jangan melonggarkan tanpa alasan eksplisit.

## Ekspektasi output
Saat menyelesaikan tugas, ringkas: apa yang diubah, file utama yang disentuh, test yang
dijalankan, test yang ditambahkan, risiko/tindak lanjut, dan update changelog.

## Done when
- Tujuan tercapai, perubahan utama terimplementasi.
- Test relevan dijalankan (dan ditambahkan bila sebelumnya belum ada/memadai); untuk perubahan
  lintas-layanan, test Laravel & Python sama-sama dijalankan.
- `docs/CODEBASE-CONTEXT.md` diperbarui bila struktur/flow berubah.
- Entri changelog ditambahkan (lihat di bawah).
- Risiko & tindak lanjut diringkas.

---

## Aturan Changelog (WAJIB)
Setiap kali membuat atau melakukan perubahan pada repo ini, **catat entri changelog** di
bagian "## Changelog" di bawah. Aturan:
- Tambahkan entri baru di **paling atas** daftar (terbaru di atas).
- Format: `- YYYY-MM-DD — <ringkas perubahan> — file/area utama — (test: <hasil>)`.
- Satu entri per perubahan logis. Tetap ringkas dan faktual.
- Jangan menghapus entri lama; changelog bersifat append-only sebagai jejak riwayat.
- Changelog ini adalah indeks ringkas semua perubahan repo.

## Changelog
- 2026-06-27 — Dokumen acuan Prompy kini append-mode seperti gambar referensi: upload berikutnya menambah daftar dokumen yang sudah dipilih sampai batas 3, label tombol menegaskan klik untuk tambah, dan composer revisi memakai "Tambah file" bukan mengganti file sebelumnya. — `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php` — (test: `php artisan test --filter=PromptStudioTest` 29 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-27 — Thumbnail gambar referensi di panel konfigurasi Prompy kini bisa diklik untuk membuka modal preview full-size yang sama dengan mode revisi, sementara tombol X tetap hanya menghapus gambar. — `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php` — (test: `php artisan test --filter=PromptStudioTest` 28 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-27 — Memperbaiki bug upload 1 gambar referensi Prompy yang langsung memunculkan error maksimal 5 file; event `change` buatan untuk sinkronisasi append file kini diabaikan oleh handler Alpine agar file tidak tergandakan, termasuk di composer revisi. — `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php` — (test: `php artisan test --filter=PromptStudioTest` 27 pass; `php -l app/Livewire/Prompts/PrompyStudio.php` pass; `npm run build` pass)
- 2026-06-27 — Lima perbaikan UX Prompy: (1) memperpanjang batas prompt IDE/REVISI/CHAT 4000/3000→12000/8000 di Laravel+Python, (2) upload gambar referensi kini append-mode (tambah tanpa menghilangkan sebelumnya) di config dan revisi, (3) tombol X per gambar untuk menghapus item dari preview tanpa menghapus semua, (4) mode revisi menampilkan thumbnail kecil mengambang (dari route owner-only baru) dengan modal full-size dan X untuk meng-exclude acuan dari revisi berikutnya (menggantikan blok teks verbose), (5) tombol Prompy kini muncul di dashboard meski user belum login (route `guest-prompy`). — `laravel/app/{Http/Controllers/Prompts/PromptReferenceImageController.php,Livewire/Prompts/PrompyStudio.php,Services/Prompts/PromptStudioService.php}`, `laravel/resources/views/{dashboard.blade.php,livewire/prompts/prompy-studio.blade.php}`, `laravel/routes/web.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `python-ai/app/{routers/prompts.py,services/prompt_generation.py}` — (test: `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|DashboardTest|ChatUiTest'` 119 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 589 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 37 pass; full `pytest` 408 pass; `npm run build` pass)
- 2026-06-24 — Menstabilkan test throttle login agar tidak flake di GitHub Actions ketika hitungan detik `RateLimiter::availableIn()` berkurang selama eksekusi Livewire; waktu test dibekukan dan cleanup rate limit dipindahkan ke `finally`. — `laravel/tests/Feature/Auth/AuthenticationTest.php` — (test: `php artisan test --filter=AuthenticationTest` 14 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 589 pass; `git diff --check` pass)
- 2026-06-24 — Mengoptimalkan Prompy untuk workshop prompt gambar/deck: label OpenAI diperbarui menjadi ChatGPT Images/GPT Image, guidance platform diselaraskan dengan kemampuan resmi OpenAI/Gemini/Canva, prompt Presentasi kini long-form tanpa batas prompt gambar biasa, dan panel hasil dikunci tetap scrollable untuk output panjang. — `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `python-ai/app/services/prompt_generation.py`, `python-ai/config/ai_config.yaml`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}`, `docs/CODEBASE-CONTEXT.md` — (test: `php -l laravel/app/Services/Prompts/PromptStudioService.php` pass; `python -m py_compile python-ai/app/services/prompt_generation.py` pass; `php artisan test --filter=PromptStudioTest` 26 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 37 pass; `php artisan test --filter='PrompyWorkspaceTest|ChatUiTest|AIUsageEventServiceTest'` 72 pass; `npm run build` pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 589 pass; full `pytest` 408 pass; `git diff --check` pass)
- 2026-06-23 — Menghapus Google Flow dari pilihan aktif Prompy dengan alias legacy ke Gemini, menambah lampiran dokumen pada composer revisi Prompy, serta menyelaraskan profil Python, UI, test, dan dokumentasi flow terbaru. — `laravel/app/{Livewire/Prompts/PrompyStudio.php,Services/Prompts/PromptStudioService.php}`, `laravel/resources/views/livewire/prompts/*`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `python-ai/config/ai_config.yaml`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}`, `docs/CODEBASE-CONTEXT.md` — (test: `php -l laravel/app/Livewire/Prompts/PrompyStudio.php` pass; `php -l laravel/app/Services/Prompts/PromptStudioService.php` pass; `php artisan test --filter=PromptStudioTest` 25 pass; `php artisan test --filter='PrompyWorkspaceTest|ChatUiTest|AIUsageEventServiceTest'` 72 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 35 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 588 pass; full `pytest` 406 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-23 — Memperkuat Prompy untuk workshop prompt gambar/PPT: menambah Google Flow, dokumen acuan PDF/DOCX/XLSX/CSV sebagai konteks sementara, catatan "Cara pakai" singkat, deck prompt kit untuk Presentasi, metadata usage aman untuk dokumen acuan, serta dokumentasi flow Prompy terbaru. — `laravel/app/{Livewire/Prompts/PrompyStudio.php,Services/Prompts/PromptStudioService.php,Services/Admin/AIUsageEventService.php}`, `laravel/resources/views/livewire/prompts/*`, `laravel/tests/{Feature/Prompts/PromptStudioTest.php,Unit/Services/Admin/AIUsageEventServiceTest.php}`, `python-ai/config/ai_config.yaml`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}`, `docs/CODEBASE-CONTEXT.md` — (test: `php -l laravel/app/Services/Prompts/PromptStudioService.php` pass; `php -l laravel/app/Livewire/Prompts/PrompyStudio.php` pass; `php artisan test --filter=PromptStudioTest` 24 pass; `php artisan test --filter=AIUsageEventServiceTest` 13 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 35 pass; `npm run build` pass)
- 2026-06-23 — Menambahkan lampiran gambar pada composer revisi Prompy: gambar versi aktif kini tampil sebagai konteks, revisi tanpa gambar baru tetap mewarisi reference images versi aktif, sedangkan upload gambar baru di composer langsung membuat versi prompt baru dengan reference images pengganti. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php -l laravel/app/Livewire/Prompts/PrompyStudio.php` pass; `php artisan test --filter=PromptStudioTest` 23 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Mengganti heuristik lokal composer Prompy dengan router AI `/api/prompts/chat` sehingga pesan seperti `apa yg kurang?`/`tesin` dijawab natural tanpa membuat versi, sedangkan arahan perubahan jelas tetap membuat revisi paket prompt. — `laravel/app/{Livewire/Prompts/PrompyStudio.php,Services/Prompts/PromptStudioService.php}`, `python-ai/app/{routers/prompts.py,services/prompt_generation.py,config_loader.py}`, `python-ai/config/ai_config.yaml`, `docs/CODEBASE-CONTEXT.md`, test Laravel/Python terkait — (test: `php artisan test --filter=PromptStudioTest` 22 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 585 pass; full `pytest` 405 pass; `npm run build` pass; `composer audit` clean; `npm audit --audit-level=high` clean)
- 2026-06-22 — Memperbaiki transcript composer Prompy agar chat biasa disimpan append-only dan tidak menghapus bubble `ISTA AI` ketika beberapa balasan punya teks sama; pertanyaan seperti `menurutmu/perlu ditingkatkan` kini dikenali sebagai diskusi tanpa membuat versi baru. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=PromptStudioTest` 21 pass; `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|ChatUiTest'` 80 pass; `git diff --check` pass)
- 2026-06-22 — Menjadikan composer Prompy intent-aware: pesan perubahan seperti ubah/tambah/ganti kembali merevisi prompt dan membuat versi baru dengan balasan chat natural, sedangkan pertanyaan/konfirmasi biasa tetap disimpan di `chat_messages` tanpa membuat versi. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=PromptStudioTest` 19 pass; `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|ChatUiTest'` 78 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Mengubah composer Prompy setelah output pertama menjadi chat-only: pesan pengguna dibalas dan disimpan di `generated_prompts.chat_messages` tanpa otomatis membuat package/versi baru; paket prompt baru hanya dibuat lewat submit konfigurasi/generate ulang. — `laravel/app/{Livewire/Prompts/PrompyStudio.php,Models/GeneratedPrompt.php}`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/database/migrations/2026_06_22_000002_add_chat_messages_to_generated_prompts_table.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=PromptStudioTest` 19 pass; `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|ChatUiTest'` 78 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Memperbaiki submit revisi Prompy agar mengikuti Memo: teks revisi langsung tampil sebagai bubble `Anda`, composer dikosongkan saat request berjalan, dan Livewire menerima revisi melalui `sendPromptRevision()` untuk membuat versi baru. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=PromptStudioTest` 19 pass; `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|ChatUiTest'` 78 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Menyamakan panel tengah Prompy Studio dengan pola Memo: setelah prompt aktif tersedia, panel berubah menjadi ringkasan prompt aktif + edit konfigurasi + dropdown versi + transcript bubble `Anda`/`ISTA AI` + composer revisi tombol bulat; form konfigurasi Prompy tidak lagi menampilkan catatan konteks tambahan; konfigurasi ulang prompt aktif membuat versi baru pada item riwayat yang sama. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/{CODEBASE-CONTEXT.md,data-flow-privacy.md}` — (test: `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|ChatUiTest'` 77 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Mengubah Prompy Studio menjadi workspace revisi setelah output pertama: panel tengah beralih dari form ke composer revisi seperti chat dengan versi v1/v2, hasil awal/revisi disimpan di `generated_prompt_versions`, upload referensi naik menjadi maksimal 5 gambar dengan thumbnail `Gambar 1-5` tanpa role/catatan, dan kontrak Python menerima `reference_images[]`, `current_package`, serta `revision_instruction`. — `laravel/app/{Livewire/Prompts/PrompyStudio.php,Models/GeneratedPrompt*.php,Services/Prompts/PromptStudioService.php,Services/Admin/AIUsageEventService.php}`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/database/migrations/2026_06_22_000001_create_generated_prompt_versions_table.php`, `python-ai/app/{routers/prompts.py,services/prompt_generation.py,config_loader.py}`, `python-ai/config/ai_config.yaml`, `docs/{CODEBASE-CONTEXT.md,data-flow-privacy.md}`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}` — (test: `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|AIUsageEventServiceTest'` 33 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 580 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 31 pass; full `pytest` 402 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Menyamakan perilaku riwayat Prompy dengan Memo: item aktif kini mengikuti `updated_at`, `activePromptId` diset canonical saat prompt terbaru/riwayat dibuka, sidebar memakai sinkronisasi Alpine setelah Livewire selesai agar highlight tidak lompat, dan section aktif otomatis terbuka. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/resources/{js/chat-page.js,views/livewire/prompts/partials/prompy-history-sidebar.blade.php}`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `DB_HOST=127.0.0.1 DB_PORT=3307 DB_DATABASE=istanaai_test php artisan test --filter=PromptStudioTest` 17 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-22 — Mengosongkan default pilihan Prompy Studio saat compose/Prompt Baru: Platform Tujuan dan Jenis Keluaran tidak lagi otomatis memilih Universal/Gambar, validasi wajib pilih ditambahkan, dan test regresi mengunci state kosong tersebut. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=PromptStudioTest` 14 pass; `npm run build` pass)
- 2026-06-21 — Menyamakan bubble output Prompy Studio: card Prompt utama dan Setelan disarankan kini memakai warna bubble muted yang sama seperti output lain, Setelan disarankan dibuat shrink-to-content tanpa whitespace Blade yang memperlebar card, serta ikon Gemini dan Canva diganti ke inline SVG berbasis asset resmi terbaru untuk ukuran card kecil. — `laravel/resources/views/livewire/prompts/{prompy-studio.blade.php,partials/prompy-platform-icon.blade.php}`, `laravel/tests/Feature/Prompts/PromptStudioTest.php` — (test: `php artisan test --filter=PromptStudioTest` 13 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Merapikan pilihan Platform Tujuan Prompy Studio: menghapus Google Flow karena overlap dengan Gemini, mengganti label Generic menjadi Universal, menambahkan ikon brand proporsional untuk GPT Image 2/Gemini/Canva serta ikon globe Universal, dan menyelaraskan kontrak profil prompt + dokumentasi privasi. — `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/resources/views/livewire/prompts/{prompy-studio.blade.php,partials/prompy-platform-icon.blade.php}`, `python-ai/config/ai_config.yaml`, `docs/{CODEBASE-CONTEXT.md,data-flow-privacy.md}`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}` — (test: `php artisan test --filter=PromptStudioTest` 13 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 28 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Memoles UX Prompy Studio: selected card platform/jenis di dark mode mempertahankan teks terang dengan outline merah, output prompt berubah dari left-line block menjadi bubble card light/dark, footer disamakan dengan Memo, upload gambar memakai spinner ikon tanpa teks loading bawah, badge `Gambar dianalisis` dihapus, dan klik riwayat prompt kini menampilkan spinner item seperti Memo. — `laravel/resources/views/livewire/prompts/{prompy-studio.blade.php,partials/prompy-history-sidebar.blade.php}`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter='PromptStudioTest|PrompyWorkspaceTest|ChatUiTest'` 72 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 576 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Memperhalus UX Prompy Studio: pilihan platform/jenis kini berpindah instan lewat state lokal Alpine, upload gambar referensi diganti menjadi card/dropzone kecil dengan status berhasil, dukungan gambar dibatasi ke JPG/PNG, teks bantuan upload dipadatkan, dan helper card `Konteks privat` dihapus. — `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=PromptStudioTest` 13 pass; `php artisan test --filter=PrompyWorkspaceTest` 3 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Menghilangkan label "Asisten Istana Pintar" dari dashboard utama, menghapus margin-top `mt-8` pada heading utama, serta menyeimbangkan proporsi vertikal dengan menaikkan letak konten utama menggunakan translasi `-translate-y-6 sm:-translate-y-10`. — `laravel/resources/views/dashboard.blade.php` — (test: `php artisan test --filter=DashboardTest` pass; `npm run build` pass)
- 2026-06-21 — Memperkuat fidelity gambar referensi Prompy Studio: prompt vision kini menghasilkan brief terstruktur yang menekankan orientasi/rasio, teks terlihat, layout, palet, serta elemen preserve/replace; generator prompt wajib mempertahankan rasio referensi dan tidak default ke 16:9 saat referensi vertikal/square; panel hasil menampilkan indikator `Gambar dianalisis`; metadata usage prompt membuka flag aman reference-image tanpa menyimpan isi prompt/brief mentah. — `python-ai/config/ai_config.yaml`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}`, `laravel/app/Services/Admin/AIUsageEventService.php`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/{Feature/Prompts/PromptStudioTest.php,Unit/Services/Admin/AIUsageEventServiceTest.php}`, `docs/CODEBASE-CONTEXT.md` — (test: `php artisan test --filter='PromptStudioTest|AIUsageEventServiceTest'` 25 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py` 28 pass; full `php -d memory_limit=-1 vendor/bin/phpunit` 575 pass; full `pytest` 399 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Menyederhanakan Prompy Studio dan mengaktifkan vision analysis untuk gambar referensi: menghapus dokumen sumber dari UI/kontrak aktif Prompy, mengirim reference image privat sebagai payload base64 internal ke Python, menganalisisnya dengan model vision terkonfigurasi (`prompt_studio.vision_models`) menjadi brief visual sebelum paket prompt dibuat, serta memperjelas dokumentasi privasi. — `laravel/app/Livewire/Prompts/PrompyStudio.php`, `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `python-ai/app/{config_loader.py,routers/prompts.py,services/prompt_generation.py}`, `python-ai/config/ai_config.yaml`, `docs/{CODEBASE-CONTEXT.md,data-flow-privacy.md}`, `README.md`, `laravel/tests/Feature/Prompts`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py}` — (test: `php artisan test --filter='PrompyWorkspaceTest|PromptStudioTest|ChatUiTest|DashboardTest'` 105 pass; `pytest tests/test_prompt_generation.py tests/test_prompt_contracts.py tests/test_app_routing.py` 31 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Memperbaiki kegagalan deploy production akibat checkout server divergen: memasukkan dua commit Prompy production-only ke history `main` agar server dapat fast-forward tanpa reset, sambil mempertahankan naming aktif `Prompts/Prompy` dan label submit ringkas "Buat Prompt". — `AGENTS.md`, `laravel/resources/js/chat-page.js`, `laravel/resources/views/livewire/prompts/prompy-studio.blade.php`, `laravel/tests/Feature/Prompts/{PrompyWorkspaceTest.php,PromptStudioTest.php}` — (test: `php artisan test --filter='PrompyWorkspaceTest|PromptStudioTest|ChatUiTest|DashboardTest'` 106 pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-21 — Memperbaiki navigasi Prompy Studio setelah fitur presentasi internal dihapus: `Prompt Baru` kini membuka state compose kosong tanpa fallback ke riwayat terbaru, klik riwayat membuka paket prompt yang dipilih, sidebar Prompy tidak lagi auto-collapse di desktop, naming aktif dipindah dari Presentation ke Prompy (`prompy` tab, `features.prompy`, `FEATURE_PROMPY`, `PrompyWorkspace`, `prompyWorkspace`) dengan alias legacy `presentation`/`presentasi` tetap diarahkan ke Prompy. — `laravel/app/Livewire/{Chat,Prompts}`, `laravel/resources/{js,views}/**/prompy*`, `laravel/config/features.php`, env examples, `laravel/tests/Feature/{Chat,Dashboard,Prompts}`, `docs/{CODEBASE-CONTEXT.md,production-config-guide.md,github-actions-cicd.md}` — (test: `php artisan test --filter='PrompyWorkspaceTest|PromptStudioTest|ChatUiTest|DashboardTest'` 106 pass; `npm run build` pass)
- 2026-06-21 — Mengubah tombol submit utama dari "Buat Paket Prompt" menjadi "Buat Prompt" agar lebih ringkas dan sesuai dengan kesepakatan visual baru. — `laravel/resources/views/livewire/presentations/prompy-studio.blade.php`, `laravel/tests/Feature/Presentations/PresentationWorkspaceTest.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php` — (test: `php artisan test --filter='PresentationWorkspaceTest|PromptStudioTest'` 15 pass)
- 2026-06-21 — Memperbaiki bug navigasi panel kiri Prompy Studio di desktop: membatasi collapse sidebar riwayat hanya jika pengguna berada pada tampilan mobile saat memulai prompt baru atau memilih item riwayat. — `laravel/resources/js/chat-page.js` — (test: `php artisan test --filter='PresentationWorkspaceTest|PromptStudioTest'` 15 pass)
- 2026-06-21 — Menyerasikan visual output panel kanan Prompy Studio: menyamakan latar belakang semua blok output menjadi abu-abu halus netral, border kiri vertikal abu-abu netral, dan font monospace agar terlihat menyatu sebagai satu kesatuan paket prompt terstruktur, mengonversi daftar "Setelan disarankan" menjadi blok teks monospace, serta menghapus sisa warna latar belakang zebra-striping belang pada kontainer luar agar latar belakang luarnya seragam putih bersih. — `laravel/resources/views/livewire/presentations/prompy-studio.blade.php` — (test: `php artisan test --filter='PresentationWorkspaceTest|PromptStudioTest'` 15 pass)
- 2026-06-21 — Menyempurnakan visual Prompy Studio: mengganti ikon tab Prompy dari PPT (projector) ke sparkles, menyederhanakan badge header panel kanan menjadi "Prompt" tanpa kata "paket", menghilangkan badge "Konteks internal" di panel kanan, serta menghapus imbuhan bahasa "(EN)" dan "(ID)" pada label prompt utama dan catatan. — `laravel/resources/views/livewire/chat/partials/chat-memo-tab-toggle.blade.php`, `laravel/resources/views/livewire/presentations/prompy-studio.blade.php`, `laravel/app/Models/GeneratedPrompt.php`, `laravel/tests/Feature/Prompts/PromptStudioTest.php`, `laravel/tests/Feature/Presentations/PresentationWorkspaceTest.php` — (test: `php artisan test --filter='PresentationWorkspaceTest|PromptStudioTest'` 15 pass; full `php artisan test` 574 pass)
- 2026-06-21 — Menghapus penuh generator PPT internal agar tab produksi fokus ke Prompy Studio: melepas model/tabel/route/controller/job/service/test PPT, endpoint renderer Python dan dependency `python-pptx`, menghapus recovery render dari deploy, menambahkan migration drop tabel artefak presentasi, serta menyelaraskan UI tab/CTA/dokumentasi ke Prompy. — `laravel/app/Livewire/Presentations/*`, `laravel/app/{Models,Jobs,Services,Http/Controllers,Policies}`, `laravel/routes/*`, `laravel/database/migrations/2026_06_21_000001_drop_presentation_generation_tables.php`, `python-ai/app/{documents_api.py,routers,services}`, env examples, `.github/workflows/ci-cd.yml`, `docs/*`, `PRD-ISTA-AI.md` — (test: `php artisan test --filter='PresentationWorkspaceTest|PromptStudioTest|DashboardTest|ChatUiTest'` 105 pass; `pytest -k 'app_routing or prompt'` 60 pass; `php -d memory_limit=-1 vendor/bin/phpunit` 574 pass; `pytest` 396 pass; `npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean; `git diff --check` pass)
- 2026-06-21 — Meningkatkan UX Presentasi & Prompy Studio: flash message auto-dismiss 5 detik dengan tombol tutup manual & pembersihan saat sub-mode berganti, sidebar riwayat otomatis melipat/collapse saat presentasi siap/dimuat untuk memperlebar area editor OnlyOffice, serta visual card riwayat Prompy Studio diselaraskan memakai ikon dokumen abu-abu netral tanpa badge "Berhasil". — `laravel/app/Livewire/Presentations/PresentationWorkspace.php`, `laravel/resources/views/livewire/presentations/*`, `laravel/resources/js/chat-page.js` — (test: `php artisan test --filter=Presentation` 78 pass; `npm run build` pass)
- 2026-06-21 — Memperbaiki akar production queue yang membuat generate PPT tidak pernah diproses: Horizon 5.45 pada Laravel 13 crash-loop karena `horizon:work` tidak menerima opsi `--stop-when-empty-for`; tambah compatibility command + binding provider agar Horizon worker hidup kembali, sambil mempertahankan generate PPT production default inline supaya submit/retry tidak bergantung pada worker. — `laravel/app/Console/Commands/CompatibleHorizonWorkCommand.php`, `laravel/app/Providers/HorizonServiceProvider.php`, `laravel/tests/Feature/Admin/HorizonWorkCompatibilityTest.php`, `docs/{CODEBASE-CONTEXT.md,production-config-guide.md}` — (test: `cd laravel && php artisan horizon:work --help` menampilkan `--stop-when-empty-for`; `cd laravel && php artisan test --filter='HorizonWorkCompatibilityTest|PresentationGenerationTest|PresentationWorkspaceTest'` 26 pass)
- 2026-06-21 — Menambah pertahanan prompt injection pada chat ISTA AI: guardrail keamanan berprioritas tertinggi (`prompts.security.guardrails`) disuntik otomatis ke paling depan setiap system prompt chat lewat chokepoint tunggal `llm_manager._stream_with_cascade`, mencakup lane general/web/RAG/knowledge + override runtime. Menahan kebocoran/printout system prompt, override instruksi ("abaikan instruksi sebelumnya"/"STOP"/role admin), role-play, penggabungan/penyusunan ulang kata, dan instruksi tersandi (Base64/ROT13/leetspeak); instruksi di dokumen/web/teks tempelan diperlakukan sebagai data. — `python-ai/config/ai_config.yaml`, `python-ai/app/config_loader.py`, `python-ai/app/llm_manager.py`, `python-ai/tests/{test_prompt_injection_guardrail.py,test_llm_streaming.py}`, `docs/CODEBASE-CONTEXT.md` — (test: `cd python-ai && source venv/bin/activate && pytest tests/test_prompt_injection_guardrail.py tests/test_llm_streaming.py tests/test_prompt_contracts.py` 30 pass; full `pytest` 460 pass; runtime check guardrail length 2526 + prepend benar)
- 2026-06-21 — Mengubah generate PPT production agar tidak lagi bergantung pada antrean worker: default `PRESENTATION_GENERATION_MODE=inline`, service menjalankan `GeneratePresentation` langsung saat submit/retry, UI langsung membuka editor saat render selesai, dan retry inline menangani error renderer dengan status `error` yang aman; mode queued tetap tersedia via env. — `laravel/config/presentations.php`, `laravel/app/Services/Presentations/PresentationGenerationService.php`, `laravel/app/Livewire/Presentations/PresentationWorkspace.php`, env examples, `docs/{CODEBASE-CONTEXT.md,production-config-guide.md}`, `laravel/tests/Feature/Presentations/{PresentationGenerationTest.php,PresentationWorkspaceTest.php}` — (test: `cd laravel && php artisan test --filter='PresentationGenerationTest|PresentationWorkspaceTest'` 25 pass; `cd laravel && php artisan test --filter='Presentation|Prompt|ChatUiTest'` 144 pass; renderer Python langsung menghasilkan PPTX valid 35.212 bytes/5 slide; `cd python-ai && source venv/bin/activate && pytest -k 'presentation'` 64 pass; full `cd laravel && php -d memory_limit=-1 vendor/bin/phpunit` 641 pass; full `cd python-ai && source venv/bin/activate && pytest` 460 pass; `cd laravel && npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean; `git diff --check` pass)
- 2026-06-21 — Memperbaiki generate PPT production yang tersangkut antrean: job `GeneratePresentation` memakai queue Redis eksplisit di production, tambah command recovery stale render untuk re-dispatch terbatas, deploy workflow memverifikasi Horizon/Scheduler dan menjalankan recovery setelah restart, env/docs/test diselaraskan. — `laravel/app/Jobs/GeneratePresentation.php`, `laravel/app/Console/Commands/RecoverStalePresentationRenders.php`, `laravel/config/presentations.php`, `.github/workflows/ci-cd.yml`, env examples, `docs/{CODEBASE-CONTEXT.md,github-actions-cicd.md}`, `laravel/tests/Feature/{Console/ResolveStalePresentationsTest.php,Presentations/PresentationGenerationTest.php}` — (test: `cd laravel && php artisan test --filter='ResolveStalePresentationsTest|PresentationGenerationTest'` 19 pass; `cd laravel && php artisan test --filter='Presentation|Prompt|ChatUiTest'` 138 pass; QA actual render via `uvicorn app.documents_api:app --port 8002` + Laravel sync queue menghasilkan status ready, 1 versi, PPTX 35.248 bytes; production queue sanity `APP_ENV=production QUEUE_CONNECTION=database` menunjukkan `presentation_queue=redis`; `cd laravel && php -d memory_limit=-1 vendor/bin/phpunit` 634 pass; `cd python-ai && source venv/bin/activate && pytest -k 'presentation or prompt'` 116 pass; `cd python-ai && source venv/bin/activate && pytest` 454 pass; `cd laravel && npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean; `git diff --check` pass)
- 2026-06-21 — Memperbaiki UX Prompy Studio: sidebar kiri kini memakai riwayat prompt khusus dengan status `Berhasil`, header/panel hasil memakai judul ringkas deterministik, dan card riwayat di bawah hasil kanan dihapus; tambah test regresi untuk sub-mode Prompy dan judul compact. — `laravel/app/Livewire/Presentations/PrompyStudio.php`, `laravel/app/Models/GeneratedPrompt.php`, `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/resources/views/livewire/presentations/*`, `laravel/tests/Feature/{Prompts/PromptStudioTest.php,Presentations/PresentationWorkspaceTest.php}`, `docs/CODEBASE-CONTEXT.md` — (test: `cd laravel && php artisan test --filter='PromptStudio|PresentationWorkspace'` 22 pass; `cd laravel && php -d memory_limit=-1 vendor/bin/phpunit` 628 pass; `cd laravel && npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean; `git diff --check` pass)
- 2026-06-21 — Menutup stuck state generate Presentasi: tambah scheduler `presentations:resolve-stale-renders` untuk mengubah pending/processing lama menjadi error recoverable, rapikan UI stale menjadi ringkas dan merah ISTA, hilangkan label `Menunggu`/teks stale dari sidebar/header, dan ubah toggle Chat/Memo/Presentasi desktop menjadi ikon-only yang expand saat hover/focus. — `laravel/app/Console/Commands/ResolveStalePresentations.php`, `laravel/routes/console.php`, `laravel/resources/views/livewire/{chat,presentations}/*`, `laravel/resources/{css/app.css,js/chat-page.js}`, `laravel/tests/Feature/{Console/ResolveStalePresentationsTest.php,Presentations/PresentationWorkspaceTest.php}`, `docs/CODEBASE-CONTEXT.md` — (test: `cd laravel && php artisan test --filter='ResolveStalePresentationsTest|PresentationWorkspaceTest'` 15 pass; `cd laravel && php artisan test --filter='Presentation|Prompt|ChatUiTest'` 132 pass; QA actual render via `uvicorn app.documents_api:app --port 8002` + Laravel `GeneratePresentation` menghasilkan status ready dan PPTX 33.544 bytes; `cd laravel && php -d memory_limit=-1 vendor/bin/phpunit` 628 pass; `cd python-ai && source venv/bin/activate && pytest -k 'presentation'` 64 pass; `cd laravel && npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean; `git diff --check` pass)
- 2026-06-21 — Menyamakan shell Presentasi/Prompy dengan pola Memo: sidebar riwayat + panel konfigurasi + panel kanan permanen, editor OnlyOffice Slides otomatis tampil untuk presentasi ready, Prompy menampilkan paket prompt di panel hasil, dan pending/processing presentasi >10 menit kini menampilkan stale-state + aksi kirim ulang job. — `laravel/app/Livewire/Presentations/{PresentationWorkspace.php,PrompyStudio.php}`, `laravel/resources/views/livewire/presentations/*`, `laravel/resources/js/chat-page.js`, `laravel/tests/Feature/{Presentations/PresentationWorkspaceTest.php,Prompts/PromptStudioTest.php}`, `docs/CODEBASE-CONTEXT.md` — (test: `cd laravel && php artisan test --filter='Presentation|Prompt'` 78 pass; `cd laravel && npm run build` pass; `cd python-ai && source venv/bin/activate && pytest -k 'presentation or prompt'` 116 pass; `cd laravel && php -d memory_limit=-1 vendor/bin/phpunit` 623 pass; `cd python-ai && source venv/bin/activate && pytest` 454 pass; `npm audit --audit-level=high` clean; `composer audit` clean; `git diff --check` pass)
- 2026-06-21 — Merilis akses Presentasi di UI: `features.presentation` aktif default dengan rollback `FEATURE_PRESENTATION=false`, dashboard user login menampilkan tombol `Buka Presentasi` menuju `/chat?tab=presentation`, template env/deploy dan dokumentasi production diselaraskan, plus test dashboard flag on/off. — `laravel/config/features.php`, `laravel/resources/views/dashboard.blade.php`, `laravel/tests/Feature/DashboardTest.php`, env examples, `docs/{CODEBASE-CONTEXT.md,production-config-guide.md}` — (test: `cd laravel && php artisan test --filter='ChatUiTest|Dashboard'` 98 pass; `cd laravel && php -d memory_limit=-1 vendor/bin/phpunit` 618 pass; `cd laravel && npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean)
- 2026-06-20 — Fase 5 epic Presentasi (#263): Prompy Studio Prompt Generator. Sub-mode `Prompy Studio` di workspace Presentasi kini berisi komponen Livewire `PrompyStudio` (state terpisah dari `Buat PPT ISTA`): input ide Bahasa Indonesia, pilih platform (GPT Image 2, Gemini/Nano Banana, Canva AI, Google Flow, Generic) & jenis (image/presentation/poster_infographic/video_storyboard), dokumen sumber owned+ready opsional dengan cuplikan `document_chunks` terbatas sebagai `source_context`, reference image privat (validasi tipe/ukuran; Python hanya menerima flag agar prompt mengingatkan upload manual di platform target), copy action main/variants/negative/settings, riwayat private owner-scoped, peringatan + flag `contains_internal_context`. Python: endpoint `/api/prompts/generate` (+`/profiles`) menyusun paket prompt JSON via LLM (main_prompt EN + notes_id ID); profil platform/jenis & template di `ai_config.yaml` (`prompt_studio`), bukan hardcode. Tidak memanggil platform AI eksternal & tidak generate gambar/video. — `python-ai/config/ai_config.yaml`, `python-ai/app/config_loader.py`, `python-ai/app/services/prompt_generation.py`, `python-ai/app/routers/prompts.py`, `python-ai/app/documents_api.py`, `python-ai/tests/{test_prompt_generation.py,test_prompt_contracts.py,test_app_routing.py}`, `laravel/app/Models/{GeneratedPrompt,AIUsageEvent}.php`, `laravel/app/Policies/GeneratedPromptPolicy.php`, `laravel/app/Services/Prompts/PromptStudioService.php`, `laravel/app/Livewire/Presentations/PrompyStudio.php`, `laravel/app/Livewire/Admin/AdminUsage.php`, `laravel/resources/views/livewire/presentations/{prompy-studio.blade.php,presentation-workspace.blade.php}`, `laravel/database/migrations/2026_06_20_000002_create_generated_prompts_table.php`, `laravel/tests/Feature/{Prompts/PromptStudioTest.php,Presentations/PresentationWorkspaceTest.php}`, `docs/{CODEBASE-CONTEXT.md,data-flow-privacy.md}` — (test: `php -d memory_limit=-1 vendor/bin/phpunit` 616 pass; `pytest` 454 pass; `npm run build` pass; `npm audit --audit-level=high` clean; `composer audit` clean)
- 2026-06-20 — Fase 5 epic Presentasi (#227): Licensed Web Asset Enrichment (opsional). Tambah `python-ai/app/services/presentation_web_assets.py` (`LicensedWebAssetService`: provider whitelist Openverse/Wikimedia Commons, kebijakan lisensi CC0/Public Domain/CC-BY/CC-BY-SA, validasi metadata wajib source/license/attribution, cache privat `storage/presentation_assets/` + sha256, fallback ke lokal saat provider/jaringan/lisensi gagal, `AssetAuditTrail`). Default tetap `local_assets_only`/no-internet: tanpa fetcher disuntik tidak ada koneksi & `enrich()` selalu None. Kontrak: `GeneratePresentationRequest` tambah `asset_mode` + `licensed_assets[]` opsional, header `X-Presentation-Asset-Mode`/`X-Presentation-Licensed-Assets`, `/templates` ekspos `asset_modes`/`default_asset_mode`/`licensed_providers`. Laravel `PresentationGenerationService` menormalkan `asset_mode` (default lokal) + meneruskan ke payload renderer. Signed-file presentasi memakai validasi HMAC canonical agar tidak rapuh terhadap order query/proxy host internal. Docs privacy/QA/CODEBASE diperbarui. — `python-ai/app/services/presentation_web_assets.py`, `python-ai/app/routers/presentations.py`, `python-ai/.gitignore`, `python-ai/tests/test_presentation_web_assets.py`, `laravel/app/Services/Presentations/PresentationGenerationService.php`, `laravel/app/Services/OnlyOffice/PresentationDocumentKey.php`, `laravel/tests/Feature/Presentations/{PresentationGenerationServiceTest,PresentationGenerationTest}.php`, `docs/{CODEBASE-CONTEXT,data-flow-privacy,presentation-visual-qa}.md` — (test: `cd laravel && php artisan test --filter=Presentation` 56 pass; `cd python-ai && source venv/bin/activate && pytest -k 'presentation'` 64 pass)
- 2026-06-20 — Fase 5 epic Presentasi (#226): OnlyOffice Slides browser editor. Tambah versioning presentasi (`presentation_versions` + `PresentationVersion`, `current_version_id` jadi versi aktif, backfill v1; job `GeneratePresentation` membuat versi baru tiap render). Editor OnlyOffice Slides (`documentType=slide`) via `PresentationWorkspace::editorConfig()` + aksi `Edit Presentasi`; `PresentationDocumentKey` diperluas (editor key per-sesi stabil, oo_token single-use, signed internal URL per-versi); callback `PresentationCallbackController` (`POST /onlyoffice/presentation-callback/{presentation}`) menyimpan PPTX terbaru (JWT exp, key segar, trusted URL, anti-replay, lock, `PptxValidator`); force-save (`PresentationForceSaveService` + endpoint `presentations.force-save`) dipicu sebelum download PPTX/PDF agar memakai versi aktif terbaru (PDF cache di-invalidasi saat edit). Namespace presentasi terpisah dari memo (key/token/cache). Memo/chat tidak diubah. — `laravel/database/migrations/2026_06_20_000002_create_presentation_versions_table.php`, `laravel/app/Models/{Presentation,PresentationVersion}.php`, `laravel/app/Jobs/GeneratePresentation.php`, `laravel/app/Services/OnlyOffice/{PresentationDocumentKey,PresentationConverter,PresentationForceSaveService,PptxValidator}.php`, `laravel/app/Http/Controllers/Presentations/{PresentationFileController,PresentationCallbackController}.php`, `laravel/app/Livewire/Presentations/PresentationWorkspace.php`, `laravel/resources/views/livewire/presentations/presentation-workspace.blade.php`, `laravel/resources/js/chat-page.js`, `laravel/routes/web.php`, `laravel/tests/{TestCase.php,Feature/Presentations/{PresentationOnlyOfficeTest,PresentationFileTest}.php}`, `docs/CODEBASE-CONTEXT.md` — (test: ditambah `PresentationOnlyOfficeTest`; verifikasi `php artisan test --filter='Presentation|OnlyOffice'` dijalankan via CI — PHP/Composer tidak tersedia di sandbox agen)
- 2026-06-20 — Fase 4 epic Presentasi (#225): Visual QA, Safety & No-Internet Asset Baseline. Tambah konstanta `ASSET_MODE='local_assets_only'`, test QA Python (no-internet via patch socket, konsistensi design token primary/accent per template, logo/header/footer per slide, batas bullet/slide/title + truncation, safety "model hanya outline" field tak dikenal diabaikan, no-overflow bounds 16:9), test unit Laravel `PresentationGenerationService` (clamp slide_count, fallback template, truncation outline), dan dokumentasi checklist QA + keputusan asset-mode/model-only-outline. Renderer inti tidak diubah. — `python-ai/app/services/presentation_assets.py`, `python-ai/tests/test_presentation_qa.py`, `laravel/tests/Feature/Presentations/PresentationGenerationServiceTest.php`, `docs/presentation-visual-qa.md`, `docs/CODEBASE-CONTEXT.md` — (test: pytest full 404 pass; full PHPUnit 589 pass via `php -d memory_limit=-1 vendor/bin/phpunit`)
- 2026-06-20 — Memperbaiki CI/CD merah (step `composer audit`) akibat 3 advisory baru (dipublikasikan 2026-06-18): upgrade `guzzlehttp/guzzle` 7.11.1→7.12.1 dan `guzzlehttp/psr7` 2.11.0→2.12.1 (CVE-2026-55767/55568/55766). Bukan dari kode fitur; audit berbasis waktu. — `laravel/composer.lock` — (test: composer audit clean; npm audit --audit-level=high clean; full PHPUnit 582 pass; npm run build pass)
- 2026-06-20 — Fase 3 epic Presentasi (#222+#223+#224): pipeline generate Laravel end-to-end. #222 `PresentationGenerationService` (render PPTX via python `/api/presentations/generate`, outline deterministik, simpan private disk) + job `GeneratePresentation` (claim-token guard pending→processing→ready/error, validasi source docs owned+ready, invalidasi pdf saat regenerate) + `FEATURE_PRESENTATION_GENERATION`. #224 `PresentationConverter` (PPTX→PDF via OnlyOffice) + `PresentationDocumentKey` (signed internal URL) + `PresentationFileController` (download PPTX/PDF owner-only, cache pdf, abort 502 saat konversi gagal) + route `presentations.{download,export.pdf,file.signed}`. #223 `PresentationWorkspace` Livewire penuh (form konfigurasi hybrid, pilih dokumen ready, status/history, download, retry, hapus; sub-mode Prompy Studio placeholder) + `PresentationLifecycleService`. — `laravel/app/Services/Presentations/*`, `laravel/app/Jobs/GeneratePresentation.php`, `laravel/app/Services/OnlyOffice/{PresentationConverter,PresentationDocumentKey}.php`, `laravel/app/Http/Controllers/Presentations/PresentationFileController.php`, `laravel/app/Livewire/Presentations/PresentationWorkspace.php`, `laravel/resources/views/livewire/presentations/presentation-workspace.blade.php`, `laravel/routes/web.php`, `laravel/app/Models/AIUsageEvent.php`, `laravel/app/Livewire/Admin/AdminUsage.php`, `laravel/tests/Feature/Presentations/{PresentationGenerationTest,PresentationFileTest,PresentationWorkspaceTest}.php`, `docs/CODEBASE-CONTEXT.md` — (test: full PHPUnit 582 pass via `php -d memory_limit=-1 vendor/bin/phpunit`; filter Presentation 32 pass; python-ai tidak disentuh. Catatan: outline MVP deterministik (pengayaan AI menyusul); OnlyOffice editor penuh tetap di #226)
- 2026-06-20 — Fase 2 epic Presentasi (#221): Python PPTX Renderer MVP — endpoint `/api/presentations/generate` (+`/templates`) di document microservice (verify_token), renderer deterministic outline→PPTX 16:9 dengan logo/header/footer personalisasi + 5 template visual (Resmi Klasik, Modern Minimal, Executive Brief, Data & Tabel, Kegiatan & Dokumentasi), local asset registry (logo emblem + ikon, no-internet), validasi template/slide_count/bullet/required fields, dependency `python-pptx==1.0.2`. — `python-ai/requirements.txt`, `python-ai/app/services/{presentation_render.py,presentation_assets.py}`, `python-ai/app/routers/presentations.py`, `python-ai/app/documents_api.py`, `python-ai/tests/{test_presentation_render.py,test_app_routing.py}`, `docs/CODEBASE-CONTEXT.md` — (test: pytest full 394 pass via venv; renderer deterministic tanpa AI/internet; Laravel tidak tersentuh)
- 2026-06-20 — Fase 1 epic Presentasi (#218): tambah tab Presentasi 3-mode di shell ISTA AI di belakang feature flag `features.presentation` + normalisasi tab (alias `presentasi`, fallback `chat`), placeholder `PresentationWorkspace`, dan data model + akses presentasi (migration `presentations`, model `Presentation` dengan helper fail-closed owned+ready, `PresentationPolicy` owner/download-ready). — `laravel/config/features.php`, `laravel/app/Livewire/Chat/ChatIndex.php`, `laravel/app/Livewire/Presentations/PresentationWorkspace.php`, `laravel/resources/views/livewire/{chat/chat-index.blade.php,chat/partials/chat-memo-tab-toggle.blade.php,presentations/presentation-workspace.blade.php}`, `laravel/resources/js/chat-page.js`, `laravel/database/migrations/2026_06_20_000001_create_presentations_table.php`, `laravel/app/Models/Presentation.php`, `laravel/app/Policies/PresentationPolicy.php`, `laravel/tests/Feature/{Chat/ChatUiTest.php,Presentations/PresentationModelTest.php}`, env examples, `docs/CODEBASE-CONTEXT.md` — (test: full PHPUnit 563 pass via `php -d memory_limit=-1 vendor/bin/phpunit`; `npm run build` pass; python-ai tidak tersentuh)
- 2026-06-18 — Menambahkan aturan workflow agar setelah push berhasil agent langsung berhenti dan tidak menunggu CI/CD atau deploy production selesai kecuali diminta eksplisit. — `AGENTS.md` — (test: n/a, dokumentasi)
- 2026-06-18 — Memperbarui lock dependency frontend untuk menutup audit npm terbaru (`dompurify` 3.4.11, `form-data` 4.0.6, `vite` 6.4.3) agar workflow CI/CD tidak berhenti di audit frontend. — `laravel/package-lock.json` — (test: `npm audit --audit-level=high` clean; `npm run build` pass)
- 2026-06-18 — Memperbaiki CSP production yang mematikan interaksi Livewire/Alpine di `/login` dengan compatibility `unsafe-eval` hanya untuk response HTML ber-marker Livewire/Alpine, plus test regresi header CSP login dan dokumentasi env. — `laravel/app/Http/Middleware/AddSecurityHeaders.php`, `laravel/config/security.php`, `laravel/tests/Feature/SecurityHeadersTest.php`, env examples, docs production/codebase — (test: SecurityHeadersTest 5 pass; full PHPUnit 549 pass via `php -d memory_limit=-1 vendor/bin/phpunit`; `php artisan test` terkena limit memori 128MB)
- 2026-06-10 — Memperbaiki kegagalan CI `npm audit` dengan upgrade devDependency `concurrently` ke ^10.0.3 sehingga `shell-quote` naik ke versi patched 1.8.4. — `laravel/package.json`, `laravel/package-lock.json` — (test: npm audit --audit-level=high clean; npm run build pass; full PHPUnit 547 pass via `php -d memory_limit=-1 vendor/bin/phpunit`)
- 2026-06-09 — Menutup celah deprovisioning akun nonaktif dengan cek `is_active` pada login reguler dan middleware `active` untuk route user terautentikasi, plus test regresi akun admin nonaktif. — `laravel/app/Http/Middleware/EnsureUserIsActive.php`, `laravel/app/Livewire/Forms/LoginForm.php`, `laravel/routes/web.php`, `laravel/routes/auth.php`, `laravel/tests/Feature/Auth/AuthenticationTest.php`, `docs/CODEBASE-CONTEXT.md` — (test: targeted Laravel 70 pass; full PHPUnit 547 pass via `php -d memory_limit=-1 vendor/bin/phpunit`; route-list middleware aktif terverifikasi; `git diff --check` pass)
- 2026-06-09 — Menghapus total fitur Google Drive (import picker + export/upload ke Drive): hapus controller/service/model/Livewire/blade/ikon Drive, route OAuth, konstanta usage `GOOGLE_DRIVE_*`, relasi `cloudStorageFiles`, dependency `google/apiclient`, key env `GOOGLE_DRIVE_*`, dan fungsi Drive di `chat-page.js`; tambah migration drop `cloud_storage_files` & `google_drive_oauth_connections`; pertahankan ekspor/unduh lokal & kolom `documents.source_*` (selalu `local`). — `laravel/app/{Http/Controllers/CloudStorage,Livewire/Chat,Models,Services/CloudStorage}`, `laravel/routes/web.php`, `laravel/config/services.php`, `laravel/resources/{js/chat-page.js,views}`, `laravel/database/migrations/2026_06_09_000001_drop_google_drive_tables.php`, `laravel/composer.{json,lock}`, `docs/CODEBASE-CONTEXT.md`, `PRD-ISTA-AI.md`, `docs/production-config-guide.md`, env examples — (test: php artisan test 547 pass; npm run build pass; composer audit clean; python-ai tidak tersentuh)
- 2026-06-09 — Menaikkan action resmi GitHub CI/CD ke major Node 24 (`actions/checkout@v6`, `actions/setup-node@v6`, `actions/setup-python@v6`) agar annotation deprecation Node 20 hilang, tetap mempertahankan env force Node 24 sebagai guard. — `.github/workflows/ci-cd.yml` — (test: action.yml resmi v6 memakai node24; git diff --check pass; workflow YAML parse pass)
- 2026-06-09 — Mengaktifkan `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` di workflow CI/CD agar action JavaScript berjalan dengan runtime Node 24 dan tidak bergantung pada Node 20 deprecated. — `.github/workflows/ci-cd.yml` — (test: git diff --check pass; workflow YAML parse pass)
- 2026-06-09 — Menambahkan GitHub Actions CI/CD untuk test Laravel/Python dan deploy otomatis production saat push ke `main` via SSH + Docker Compose, termasuk dokumentasi secret dan update panduan deploy. — `.github/workflows/ci-cd.yml`, `.gitignore`, `docs/github-actions-cicd.md`, `docs/deploy-digitalocean.md`, `docs/CODEBASE-CONTEXT.md` — (test: git diff --check pass; workflow YAML parse pass; composer validate/audit pass; npm audit/build pass; PHPUnit 590 pass; pytest 378 pass)
- 2026-06-09 — Upgrade framework Laravel 11.54 → 13.14 (laravel/framework ^13, laravel/tinker ^3, Symfony 7→8, polyfill php84/php86), naikkan minimum PHP root ke `^8.3`, dan hapus audit-ignore CVE-2026-48019 yang kini tertambal di L13. Bersihkan artefak lokal regenerable (.DS_Store, __pycache__, .pytest_cache, network.log, python-ai/{fastapi,server}.log, truncate laravel.log, .phpunit.result.cache). — `laravel/composer.json`, `laravel/composer.lock` — (test: php artisan test 590 pass; npm run build pass; composer audit clean; python-ai tidak tersentuh)
- 2026-06-09 — Mengubah workflow repo menjadi langsung commit/push ke `main` dan deploy tanpa folder `issue/` maupun PR flow, serta menyesuaikan README. — `AGENTS.md`, `README.md`, `docs/testing-guide.md`, `docs/workflow-review.md`, `issue/` — (test: n/a, dokumentasi; `git diff --check` pass)
- 2026-06-09 — Memperbaiki bug lintas Laravel-python-ai untuk export PDF link aman, sync status knowledge Chroma, stale job knowledge, gate import Google Drive/upload lokal, dan healthcheck readiness production. — `laravel/app/Services/Knowledge/KnowledgeLifecycleService.php`, `laravel/app/Jobs/ProcessKnowledgeDocument.php`, `laravel/app/Services/DocumentLifecycleService.php`, `laravel/app/Services/CloudStorage/GoogleDriveService.php`, `python-ai/app/routers/knowledge.py`, `python-ai/app/services/document_export.py`, `python-ai/app/services/rag_ingest.py`, `docker-compose.production.yml`, `docs/CODEBASE-CONTEXT.md` — (test: targeted Python 49 pass; targeted Laravel 59 pass; full PHPUnit 590 pass via `php -d memory_limit=-1 vendor/bin/phpunit`; full pytest 378 pass; `git diff --check` pass)
- 2026-06-09 — Menyelaraskan `AGENTS.md` agar konsisten dengan template `AGENTS.md` istura-app (struktur: Tentang repo, WAJIB dibaca dulu, Cara bekerja, Verifikasi wajib + Perintah cepat, Keamanan, Ekspektasi output, Done when, Changelog). — `AGENTS.md` — (test: n/a, dokumentasi)
- 2026-06-08 — Mengunci Google Drive OAuth pusat agar wajib active admin/super-admin selain setup key/allowlist email, dan mengetatkan trusted URL callback OnlyOffice ke scheme/host/port exact. — `laravel/app/Services/CloudStorage/GoogleDriveOAuthService.php`, `laravel/app/Http/Controllers/OnlyOfficeCallbackController.php`, `laravel/tests/Feature/CloudStorage/GoogleDriveCentralOAuthTest.php`, `laravel/tests/Feature/Memos/OnlyOfficeCallbackTest.php`, `docs/CODEBASE-CONTEXT.md`, `docs/production-config-guide.md`, `deploy/digitalocean.env.example` — (test: targeted PHPUnit 43 pass; full PHPUnit 584 pass via `php -d memory_limit=-1 vendor/bin/phpunit`; pytest 374 pass; composer audit 1 ignored mitigated; Vite build pass)
- 2026-06-08 — Hardening security audit: tutup public registration default production, kunci Horizon ke super-admin aktif, hilangkan `unsafe-eval` default production, mitigasi email CRLF CVE Laravel 11, update dependency Laravel/Symfony, dan test Chroma internal-only. — `laravel/`, `python-ai/tests/test_production_compose_security.py`, `docker-compose.production.yml`, `docs/production-config-guide.md` — (test: PHPUnit 580 pass; pytest 374 pass; composer audit 1 ignored mitigated; npm audit clean; pip-audit 1 Chroma no fixed version, mitigated internal-only)
- 2026-06-08 — Menambahkan `PRD-ISTA-AI.md` (PRD lengkap: overview, requirements, core features, user/admin flow, arsitektur, skema DB, constraints, acceptance criteria) untuk aplikasi testing eksternal. — `PRD-ISTA-AI.md` — (test: n/a, dokumentasi)
- 2026-06-08 — Menambahkan `docs/CODEBASE-CONTEXT.md` (peta Laravel + python-ai, fitur, flow user & admin) dan menambahkan bagian "WAJIB dibaca dulu (context)" + aturan changelog di `AGENTS.md`. — `AGENTS.md`, `docs/CODEBASE-CONTEXT.md` — (test: n/a, dokumentasi)
